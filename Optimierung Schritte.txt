# Schritt 1 — Prüfen, dass nur ein Publisher aktiv ist

Bitte zuerst diese Befehle ausführen:

sudo docker exec -it mxck2_control bash -lc "source /opt/ros/humble/setup.bash && source /mxck2_ws/install/setup.bash && ros2 node list | sort"
sudo docker exec -it mxck2_control bash -lc "source /opt/ros/humble/setup.bash && source /mxck2_ws/install/setup.bash && ros2 topic info /autonomous/ftg/gap_angle -v"
sudo docker exec -it mxck2_control bash -lc "source /opt/ros/humble/setup.bash && source /mxck2_ws/install/setup.bash && ros2 topic info /autonomous/ftg/target_speed -v"
sudo docker exec -it mxck2_control bash -lc "source /opt/ros/humble/setup.bash && source /mxck2_ws/install/setup.bash && ros2 topic info /autonomous/ackermann_cmd -v"
Erwartung

Je FTG-Topic idealerweise:

1 Publisher für /autonomous/ftg/gap_angle
1 Publisher für /autonomous/ftg/target_speed
1 Publisher für /autonomous/ackermann_cmd aus deinem FTG-Control-Node

Wenn da mehrere stehen, müssen wir das zuerst bereinigen.

# Schritt 1 — Prüfen, dass nur ein Publisher aktiv ist

Bitte zuerst diese Befehle ausführen:

sudo docker exec -it mxck2_control bash -lc "source /opt/ros/humble/setup.bash && source /mxck2_ws/install/setup.bash && ros2 node list | sort"
sudo docker exec -it mxck2_control bash -lc "source /opt/ros/humble/setup.bash && source /mxck2_ws/install/setup.bash && ros2 topic info /autonomous/ftg/gap_angle -v"
sudo docker exec -it mxck2_control bash -lc "source /opt/ros/humble/setup.bash && source /mxck2_ws/install/setup.bash && ros2 topic info /autonomous/ftg/target_speed -v"
sudo docker exec -it mxck2_control bash -lc "source /opt/ros/humble/setup.bash && source /mxck2_ws/install/setup.bash && ros2 topic info /autonomous/ackermann_cmd -v"
Erwartung

Je FTG-Topic idealerweise:

1 Publisher für /autonomous/ftg/gap_angle
1 Publisher für /autonomous/ftg/target_speed
1 Publisher für /autonomous/ackermann_cmd aus deinem FTG-Control-Node

Wenn da mehrere stehen, müssen wir das zuerst bereinigen.

mv /home/mxck/development_ws/src/follow_the_gap_v0 /home/mxck/backup_old_ftg_ws/ 2>/dev/null
mv /home/mxck/development_ws/src/follow_the_gap_v0_ride /home/mxck/backup_old_ftg_ws/ 2>/dev/null
mv /home/mxck/development_ws/src/auto /home/mxck/backup_old_ftg_ws/ 2>/dev/null
mv /home/mxck/development_ws/src/obstacle_substitution /home/mxck/backup_old_ftg_ws/ 2>/dev/null
mv /home/mxck/development_ws/src/mxck_obstacle_substitution /home/mxck/backup_old_ftg_ws/ 2>/dev/null
mv /home/mxck/development_ws/src/lidar_vs /home/mxck/backup_old_ftg_ws/ 2>/dev/null
mv /home/mxck/development_ws/src/ftg_scan_filter /home/mxck/backup_old_ftg_ws/ 2>/dev/null

Schritt 3 — Alte Build-/Install-Artefakte löschen

Danach die alten Artefakte in build, install, log löschen:

rm -rf /home/mxck/development_ws/build/follow_the_gap_v0
rm -rf /home/mxck/development_ws/build/follow_the_gap_v0_ride
rm -rf /home/mxck/development_ws/build/auto
rm -rf /home/mxck/development_ws/build/obstacle_substitution
rm -rf /home/mxck/development_ws/build/mxck_obstacle_substitution
rm -rf /home/mxck/development_ws/build/lidar_vs
rm -rf /home/mxck/development_ws/build/ftg_scan_filter

rm -rf /home/mxck/development_ws/install/follow_the_gap_v0
rm -rf /home/mxck/development_ws/install/follow_the_gap_v0_ride
rm -rf /home/mxck/development_ws/install/auto
rm -rf /home/mxck/development_ws/install/obstacle_substitution
rm -rf /home/mxck/development_ws/install/mxck_obstacle_substitution
rm -rf /home/mxck/development_ws/install/lidar_vs
rm -rf /home/mxck/development_ws/install/ftg_scan_filter

Optional auch alte Logs löschen:

rm -rf /home/mxck/development_ws/log
rm -rf /home/mxck/development_ws/src/log

########  #########################  #########################   ######################
5) Was du nach dem Merge praktisch machen sollst

Wenn der technische Fix-PR gemerged ist, dann kommt dein echter Plattform-Test.

Terminal A — vehicle_control wie bisher starten

Nutze deine bekannte funktionierende Reihenfolge im Container.
Minimal:

source /opt/ros/$ROS_DISTRO/setup.bash
source /mxck2_ws/install/setup.bash
ros2 launch vehicle_control manual_control_launch.py

Dann:

Deadman prüfen
Kalibrierung abschließen
Manual testen
Terminal B — echten Build des FTG-Stacks
source /opt/ros/$ROS_DISTRO/setup.bash
source /mxck2_ws/install/setup.bash
cd /mxck2_ws

colcon build --symlink-install --packages-select \
  obstacle_msgs \
  obstacle_substitution \
  follow_the_gap_v0 \
  mxck_ftg_perception \
  mxck_ftg_planner \
  mxck_ftg_control \
  mxck_ftg_bringup

Dann:

source /mxck2_ws/install/setup.bash
Terminal C — FTG-Stack starten
source /opt/ros/$ROS_DISTRO/setup.bash
source /mxck2_ws/install/setup.bash

ros2 launch mxck_ftg_bringup ftg_full_system.launch.py \
  use_tf:=true \
  use_ftg_stack:=true \
  use_scan_preprocessor:=true \
  record_bag:=true \
  bag_name:=ftg_vehicle_pretest_01
6) Was du vor dem echten Fahrzeugtest prüfen musst
Nodes
ros2 node list | sort

Erwartet ungefähr:

/follow_the_gap
/obstacle_substitution
/scan_preprocessor_node
/ctu_ftg_adapter_node
/ftg_command_node
Wichtige Topics
ros2 topic list | grep -E "scan|obstacles|final_heading_angle|gap_found|autonomous/ftg|ackermann"
Einzeltests
ros2 topic echo /gap_found
ros2 topic echo /final_heading_angle
ros2 topic echo /autonomous/ftg/gap_angle
ros2 topic echo /autonomous/ftg/target_speed
ros2 topic echo /autonomous/ackermann_cmd
Ganz wichtig: nur ein Publisher auf Final Command
ros2 topic info /autonomous/ackermann_cmd -v

Da darf vor deinem Test kein Chaos sein.